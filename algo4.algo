PROCEDURE bubble-sort (var arr: ARRAY_OF INTEGER)
VAR

    set_declarations
BEGIN
    FOR i FROM 1 TO arr. lenght -1  DO

    current := arr[i];
    j:=i-1;
    WHILE(j>0 AND arr[j]> current) DO
    arr[j+1] :=arr[j]
    j:=j-1
    END_WHILE
    arr[j+1]=current;

END_FOR
END